<?xml version="1.0" encoding="utf-8"?>
<appengine-web-app xmlns="http://appengine.google.com/ns/1.0">
  <application>emavalidator</application>
  <version>6</version>
  <!-- <version>5</version> -->

  <!-- App Engine to send multiple requests to one instance in parallel: -->
  <threadsafe>false</threadsafe>
  <instance-class>B8</instance-class>
  <basic-scaling>
    <max-instances>50</max-instances>
    <idle-timeout>10m</idle-timeout>
  </basic-scaling>

  <!-- Configure serving/caching of GWT files -->
  <static-files>
    <include path="**" />

    <!-- The following line requires App Engine 1.3.2 SDK -->
    <include path="**.nocache.*" expiration="0s" />
    
    <!-- A standard robots.txt file for now, should prevent error message from main Google Search results -->
    <include path="/robots.txt" />

    <include path="**.cache.*" expiration="365d" />
    <exclude path="**.gwt.rpc" />
  </static-files>

  <!-- Configure java.util.logging -->
  <system-properties>
    <property name="java.util.logging.config.file" value="WEB-INF/logging.properties"/>
  </system-properties>

  <!-- HTTP Sessions are disabled by default. To enable HTTP sessions specify: -->

  <sessions-enabled>true</sessions-enabled>

  <!-- It's possible to reduce request latency by configuring your application to asynchronously write HTTP session data to the datastore:  -->

  <async-session-persistence enabled="true" />

  <!--  
  With this feature enabled, there is a very small chance your app will see
  stale session data. For details, see
  http://code.google.com/appengine/docs/java/config/appconfig.html#Enabling_Sessions  -->
</appengine-web-app>